# Retained training components

- `dapo/`: DAPO trainer, group filtering, and original configuration examples.
- `code_sandbox/`: local code reward execution service and usage notes.

Other upstream recipes were removed from this focused experimental repository.
The core verl library remains intact. For new runs, use `scripts/train.py`.
