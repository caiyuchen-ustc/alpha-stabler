# DAPO trainer

This directory contains the DAPO trainer and configuration used by the shared
launcher. Run `python scripts/train.py rl math --rl-algorithm dapo` from the
repository root. Set local model/data paths as described in the root README.

Upstream license and copyright notices are retained in the source files.
